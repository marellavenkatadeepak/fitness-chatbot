import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({
  subsets: ["latin"],
  variable: "--font-inter",
});

export const metadata: Metadata = {
  title: "FitCoach AI - Your Personal Fitness Coach",
  description:
    "Get personalized workout plans, nutrition advice, and fitness coaching powered by AI. Your 24/7 personal trainer.",
  keywords: ["fitness", "AI coach", "workout", "nutrition", "personal trainer"],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" className="dark">
      <body className={`${inter.variable} font-sans`}>{children}</body>
    </html>
  );
}
